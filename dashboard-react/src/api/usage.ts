import { adminGet } from './client'; import type { DailyStats, ListResponse, Stats, UsageAggregate, UsageLog } from '../types/api'
export const overview = () => adminGet<Stats>('/stats/overview', { start_time: new Date(Date.now() - 6 * 86400000).toISOString(), end_time: new Date().toISOString() }); export const logs = () => adminGet<ListResponse<UsageLog>>('/usage-logs', { page: 1, page_size: 50 }); export const daily = () => adminGet<ListResponse<DailyStats>>('/stats/daily', { page: 1, page_size: 100 }); export const usageStats = (group_by: 'user' | 'api_key' | 'model' | 'channel') => adminGet<ListResponse<UsageAggregate>>('/stats/usage', { group_by })
