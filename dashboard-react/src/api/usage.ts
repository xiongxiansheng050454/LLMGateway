import { adminGet } from './client'; import type { ListResponse, UsageLog, Stats } from '../types/api'
export const overview = () => adminGet<Stats>('/stats/overview'); export const logs = () => adminGet<ListResponse<UsageLog>>('/usage-logs', { page: 1, page_size: 50 }); export const daily = () => adminGet<ListResponse<Record<string, unknown>>>('/stats/daily', { page: 1, page_size: 100 }); export const usageStats = (group_by: string) => adminGet<ListResponse<Record<string, unknown>>>('/stats/usage', { group_by })
